import numpy as np

def bird_in_flight(n = 20000):
  data.frame(i = seq(1, 2000, 2000/n)   
    x = 3*(np.sin(2*np.pi*i/2000)**3)
    y = np.cos(8*np.pi*i/2000)
    xend = (3.0/2)*(np.sin(2*np.pi*i/2000)**3)
    yend = (-1.0/2)*np.cos(6*np.pi*i/2000)
    data =  [x, y]
    data = np.array(data).T
    return(data)
